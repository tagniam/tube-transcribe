import { TrackMouseDirective } from './track-mouse.directive';

describe('TrackMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackMouseDirective();
    expect(directive).toBeTruthy();
  });
});
